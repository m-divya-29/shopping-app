export class Ingredient{
constructor(public name: string, public amount: number) {} // this is shortcut to manually adding these attributes to the class and assigning this.name = name and this.amount = amount. 
}